const ask = require('readline-sync');


/*const dogs = ["Cane Corso", "Pitbull", "Pinscher", "Dalmata", "Salsicha"]
let choice = ask.question("Digite um numero de 0 a 4: ");
console.log(dogs[choice]); 


const listaDeCompras = ['Abacate', 'Banana', 'Tomate'];
console.log(listaDeCompras[1]);

const numbers = [1, 2, 3, 4, 5, 6];
console.log(numbers)
console.log(`O array numbers tem ${numbers.length} parametros`);
numbers.push(7)
console.log(numbers)
numbers.splice(3,2);
console.log(numbers)
console.log(`O array sem 4 e 5 tem ${numbers.length} parametros`)*/